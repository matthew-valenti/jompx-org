schema: ./schema.graphql
documents: 'apps/**/!(*.d).{ts,tsx,graphql}'
generates:
  ./schema.graphql.types.ts:
    plugins:
      - typescript
      - typescript-operations
      - typed-document-node
  ./schema.graphql.json:
    plugins:
      - introspection