// TODO: Does Jest run the .test or do I need to use spec file?
// import { config } from './config';

// describe('config', () => {
//   it('should work', () => {
//     expect(config()).toEqual('config');
//   });
// });
