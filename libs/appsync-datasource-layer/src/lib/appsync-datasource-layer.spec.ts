import { appsyncDatasourceLayer } from './appsync-datasource-layer';

describe('appsyncDatasourceLayer', () => {
  it('should work', () => {
    expect(appsyncDatasourceLayer()).toEqual('appsync-datasource-layer');
  });
});
